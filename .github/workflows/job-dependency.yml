name: Job-seq
on:
  workflow_dispatch: null
jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - run: echo"Hello this is job 1"
  job-2:
    runs-on: ubuntu-latest
    needs: [job-1, job-3]
    steps:
      - run: echo "Hello this is job 2"
  job-3:
      runs-on: ubuntu-latest
      steps:
        - run: echo "Hello there,this is job 3"
